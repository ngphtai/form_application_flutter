part of 'constants.dart';

/// [ParamsConstants] file contains the variables.
/// It is just only using for parameters REQUEST API
/// Keys of params using for request API or key of response when
/// request API is COMPLETED
/// It is used as an alternative to creating models with responses that
/// are too simple, containing only a single root object
/// key of response body
class ParamsConstants {
  /// key of email when request/response API
  static const email = "email";

  /// key of password when request/response API
  static const password = "password";
}
